# C++ Programming & Problem Solving Practice 🚀

Welcome to my C++ Practice Repository! This repository documents my programming journey, basic problem-solving, and foundational C++ concepts.

---

## 📂 Repository Structure

### 1. 🔀 Conditionals & Branching Logic
| File | Description |
| :--- | :--- |
| [`even_or_odd.cpp`](even_or_odd.cpp) | Checks whether an entered integer is Even or Odd using the modulus operator (`%`). |
| [`student_grade_calculator.cpp`](student_grade_calculator.cpp) | Calculates subject marks, total percentage, and assigns grades (`Grade A`, `Grade B`, Pass/Fail). |

### 2. 🔁 Loops & Iterations (For & While)
| File | Description |
| :--- | :--- |
| [`sum_even_numbers_for_loop.cpp`](sum_even_numbers_for_loop.cpp) | Calculates the sum of all even numbers from `1` to `N` using a `for` loop. |
| [`sum_numbers_while_loop.cpp`](sum_numbers_while_loop.cpp) | Calculates the cumulative sum of numbers from `1` to `N` using a `while` loop. |
| [`print_odd_numbers_while.cpp`](print_odd_numbers_while.cpp) | Takes input from user and prints all odd numbers up to `N` using a `while` loop. |
| [`multiples_of_3_for_loop.cpp`](multiples_of_3_for_loop.cpp) | Prints all numbers divisible by 3 up to `100` using a `for` loop. |
| [`multiples_of_3_while_loop.cpp`](multiples_of_3_while_loop.cpp) | Prints all numbers divisible by 3 up to `20` using a `while` loop. |
| [`loop_break_statement.cpp`](loop_break_statement.cpp) | Demonstrates loop control using the `break` statement. |

### 3. ⬛ Nested Loops & Patterns
| File | Description |
| :--- | :--- |
| [`nested_loop_pattern.cpp`](nested_loop_pattern.cpp) | Prints a 2D rectangular grid of asterisks (`*`) using nested `for` loops. |
| [`pattern_number_square.cpp`](pattern_number_square.cpp) | Generates a 2D square matrix/grid of sequential numbers. |
| [`pattern_alphabet_square.cpp`](pattern_alphabet_square.cpp) | Prints a 3x3 square grid of continuous alphabets (`A` to `I`) using nested loops. |

### 4. 🔢 Basics, Typecasting & I/O
| File | Description |
| :--- | :--- |
| [`billing_system.cpp`](billing_system.cpp) | Calculates item bill, total items, and average unit price using `double` and `iomanip`. |
| [`typecasting_float_to_int.cpp`](typecasting_float_to_int.cpp) | Demonstrates explicit typecasting from `float` to `int`. |
| [`user_input_greeting.cpp`](user_input_greeting.cpp) | Takes user input string and prints a personalized greeting message. |

---

## 🛠️ How to Compile & Run

You can compile any `.cpp` file using `g++`:

```bash
# Example: Compile
g++ billing_system.cpp -o billing_system.exe

# Run executable
./billing_system.exe
```

---
*Maintained with ❤️ by Ismail Khan*
